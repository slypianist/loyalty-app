name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build Docker image
        run: docker-compose build
      - name: Start Docker containers
        run: docker-compose up -d
    if: success()
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.2.0
        with:
          host: 45.33.19.65
          username: sylvester
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            rsync -avz --delete ./ sylvester@45.33.19.65:/home/sylvester/loyalty
